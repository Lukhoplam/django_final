Installation
============

To install My Project, you can use `pip`. Open a terminal and run:

.. code-block:: bash

    pip install django_project

This will install the latest version of My Project and its dependencies.

